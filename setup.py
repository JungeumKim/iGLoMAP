from setuptools import setup
from setuptools import find_packages

setup(
    name='iglo',
    version='0.1.0',
    author='anonymous authors',
    packages=find_packages(),
    description='My project will be super faster by setting up this package'
)

